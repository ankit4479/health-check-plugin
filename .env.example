# health-check-plugin — environment variables
# Copy to .env and fill in. .env is gitignored; never commit real secrets.
# Each var is referenced BY NAME from health-check.config.json (urlEnv / webhookEnv /
# repoEnv / tokenEnv / headersFromEnv), so you can rename them to suit your project.

# ── Data sources (postgres collectors) ──────────────────────────────
# Referenced by config.dataSources[*].urlEnv
DATABASE_URL=postgresql://user:pass@host:5432/dbname

# ── Delivery channels (you can run several at once) ─────────────────
# Referenced by config.channels[*].webhookEnv. Reports AND healing outcomes
# (including PR links) are broadcast to every configured channel.
HEALTH_DISCORD_WEBHOOK_URL=
HEALTH_SLACK_WEBHOOK_URL=

# ── GitHub issue creation ───────────────────────────────────────────
# Referenced by config.github.repoEnv / tokenEnv
HEALTH_GITHUB_REPO=owner/repo
# Optional: a PAT with `repo` scope. If omitted, the local `gh` CLI is used instead.
GITHUB_TOKEN=

# ── Interactive bot (only if config.bot.enabled = true) ─────────────
# The bot is a 24/7 process with IN-CHANNEL buttons (not a webhook).
# Discord: a bot token from a Discord application (NOT a webhook URL).
HEALTH_DISCORD_BOT_TOKEN=
# Slack: a bot token (xoxb-…) and an app-level token (xapp-…) for Socket Mode.
HEALTH_SLACK_BOT_TOKEN=
HEALTH_SLACK_APP_TOKEN=

# ── Config path override (optional) ─────────────────────────────────
# HEALTH_CHECK_CONFIG=./health-check.config.json
